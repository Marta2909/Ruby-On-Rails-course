<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

  <nav class="navbar navbar-inverse">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <a class="navbar-brand" href="#">Blog</a>
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li <% if params[:controller] == "home" && params[:action] == "index" %> class="active" <% end %>><a href="/">Strona główna <span class="sr-only">(current)</span></a></li>
          <li <% if params[:controller] == "home" && params[:action] == "about" %> class="active" <% end %>><a href="<%= about_path %>">O blogu</a></li>
          <li <% if params[:controller] == "home" && params[:action] == "contact" %> class="active" <% end %>><a href="<%= contact_path %>">Skontaktuj się</a></li>
        </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav>

    <div class="container">
      <%= yield %>
    </div>

    <footer>
      <p>stopka</p>
    </footer>
  </body>
</html>
